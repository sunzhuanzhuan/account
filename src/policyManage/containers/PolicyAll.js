import React, {  } from 'react';
import PolicyCard from "@/policyManage/components/PolicyCard";

import './PolicyAll.less'

const PolicyAll = (props) => {
  return (
    <div>
      <PolicyCard/>
    </div>
  );
};

export default PolicyAll;
